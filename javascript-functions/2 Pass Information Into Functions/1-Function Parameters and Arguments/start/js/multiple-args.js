function getAreaOfTriangle(width, length) {
    const area = width * length;
    console.log('The area of the triangle is ' + area);
}

getAreaOfTriangle(10, 30);
getAreaOfTriangle(20, 30);
getAreaOfTriangle(15, 86);

